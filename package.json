{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.12"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "deploy": "npm run build && gh-pages -d docs/.vitepress/dist -b gh-pages",
    "preview": "vitepress preview docs"
  }
}